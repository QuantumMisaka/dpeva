---
title: DeepMD-kit 3.2 Compatibility Qualification
status: qualified-with-boundary
audience: Compatibility Owner / Scientific Owner / Developers
last-updated: 2026-09-05
owner: Compatibility Owner
---

# DeepMD-kit 3.2 Compatibility Qualification

## Decision

This report records the evidence available for the DeepMD-kit 3.2
compatibility lane. It is a bounded engineering qualification record, not a
scientific performance result. Three explicitly implemented DPA4 PT
capabilities are promoted to `supported` from complete CPU and SAI evidence;
the DPA4C result remains experimental because the tested artifact is not a
verified DPA4C model-family artifact. All other
capabilities remain deliberately bounded by their current status.

The compatibility manifest and this report must be updated together. A
DeepMD version number alone is never sufficient for promotion.

## Manifest status distribution

The current 17-record manifest (`src/dpeva/compatibility/deepmd-3.2.json`)
has the following explicit distribution:

| Status | Records |
|---|---:|
| `supported` | 3 |
| `experimental` | 8 |
| `unsupported` | 4 |
| `blocked-upstream` | 2 |

The two CPU non-PBC DPA4C records (`eval-desc` and `inference`) remain
`blocked-upstream` pending [DeepMD-kit issue #6002](https://github.com/deepmodeling/deepmd-kit/issues/6002)
and a fixed-version non-PBC contract. Neither record is promoted by this
report.

## CPU contract evidence

The SAI CPU-contract job `1128442` ran in the exact `dpeva-dpa4-320`
environment and produced **11 passed / 0 skipped**. Three promotion-grade CPU
attestations are retained under `docs/reports/evidence/deepmd-3.2/`; the
single selected model contract covers the declared `regular` model path.
This is a contract result, not a scientific accuracy or general model-family
claim.

The CI contract lane is intentionally stricter: it downloads a protected
fixture bundle, verifies the configured URL and SHA-256, sets
`DPEVA_DEEPMD_CONTRACT_REQUIRED=1`, and rejects missing fixtures or skipped
cases. The required command is:

```text
pytest -m deepmd_contract tests/contract/deepmd -q
```

The local fixture skips cannot be substituted for that CI-required positive
fixture evidence.

## SAI V100 qualification

The authorized bounded SAI qualification completed successfully:

| Field | Recorded value |
|---|---|
| JobID | `1128260` |
| Immutable external directory | retained on SAI; omitted from the public repository |
| Scheduler result | `COMPLETED` |
| Elapsed / exit code | completed / `0:0` |
| Node / GPU | SAI 4V100 / `Tesla V100-SXM2-32GB` |
| Intended environment | `dpeva-dpa4-320`, exact DeepMD-kit 3.2.0 |

The collector completed with `--require-complete` and produced seven finished
attestations: regular and EMA variants for `pt test`, `pt eval-desc`, and
`pt embed`, plus a periodic `pt-expt eval-desc` run whose artifact was later
rejected for DPA4C promotion. A post-qualification inspection with `dp --pt
show MODEL descriptor` identified every branch, including `downstream`, as
descriptor type `dpa4`, not `dpa4c`. The full raw aggregate remains on SAI;
its SHA-256 is
`b3a729a37f72ee9f8de21e34ef0450b93bf0b30a03fcc4d79686f4a6f3ff7f83`.
The repository retains only the sanitized promotion aggregate
`docs/reports/evidence/deepmd-3.2/sai-qualification-job-1128260-public.json`,
which contains the six valid DPA4 attestations and the raw evidence hash.
It proves command/environment completion on the selected V100 lane; it is not
a scientific precision or universal GPU support statement. The DPA4C
attestation remains in the external raw record only and cannot be consumed by
the repository promotion gate. A true DPA4C artifact must pass family
preflight before new evidence is issued.

The launch protocol records and rechecks these identities before scientific
commands: the input JSON SHA-256, Slurm script SHA-256, regular and EMA
checkpoint SHA-256 values, and the separately declared DPA4C-model SHA-256.
In this run the declared DPA4C path resolved to the same
`a36fe46cb707f735904dcc9a56529718b21fbcfc11cfc7009e0a8245f1d9ab9a`
artifact as the regular DPA4 checkpoint. That equality, together with the
descriptor inspection, is why the DPA4C-labelled attestation is rejected.

The actual resource contract was `4V100`, one node, one task, one V100,
`improper-gpu`, and a 30-minute limit; Job `1128260` completed in 36 seconds.
The ordinary development environment remained separate from the exact
`dpeva-dpa4-320` qualification environment.

## Promotion rule and conclusion

Promotion requires a producer-issued per-capability attestation (or a
qualification aggregate containing such attestations) whose exact manifest
key, verification command, `DeePMD-kit v3.2.0`, and finished/zero-return-code
fields match. Repository-local references must resolve to JSON files; report
anchors and raw command records are insufficient. A capability whose
`required_evidence` includes `sai-v100-qualification` additionally requires a
numeric JobID and V100 identity, and its complete `sai_verification_cases`
set must be present exactly once. Qualification aggregates are bound to the
launch input SHA and its manifest-derived attestation specs; stale specs fail
collection instead of being reinterpreted against the current manifest.
Unsupported and blocked-upstream records cannot be promoted by an
environment/version check.

The collector treats the verified `environment/gpu.json` value as the sole
GPU identity. An optional CLI GPU value is only an expected diagnostic and
must match the recorded value after whitespace normalization; mismatches,
non-numeric or inconsistent JobIDs, empty bound specs, and any per-spec
attestation construction anomaly produce `status=failed` with zero finished
attestations.

The present evidence satisfies the promotion gate for exactly three records:
DPA4 `pt test`, `pt eval-desc`, and `pt embed`. Their manifest entries point
to the corresponding CPU attestation and the shared SAI aggregate. Keep the
periodic DPA4C `pt-expt eval-desc` record experimental with no promotion
reference: `--pt-expt` command success alone does not establish the DPA4C model
family. A future promotion requires a genuine DPA4C artifact and family
preflight. Keep the remaining eight experimental records unchanged,
the four policy `unsupported` records unchanged, and both non-PBC records
blocked by issue #6002.

## Retry and ownership boundary

The successful SAI qualification supersedes the earlier cancelled diagnostic
attempt (`1126627`); that earlier attempt remains historical evidence only.
Future promotions require a new bounded job, complete immutable
`qualification.json`, and `--require-complete` before any manifest edit.

## Evidence references

- Capability declaration: `src/dpeva/compatibility/deepmd-3.2.json`
- CPU contract harness: `tests/contract/deepmd/`
- SAI preparation/submission/collection: `scripts/validation/*deepmd_32_qualification*`
- Qualification template: `docs/reports/templates/deepmd-3.2-qualification.md`
- Sanitized SAI promotion aggregate: `docs/reports/evidence/deepmd-3.2/sai-qualification-job-1128260-public.json`
- CPU attestations: `docs/reports/evidence/deepmd-3.2/cpu-job-1128442-attestations/`
- SAI job reference: the recorded external directory and JobID above
